# Point this at your running FastAPI backend
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Required when the backend sets LIFTGUARD_API_KEY. This is used by the
# browser for REST and WebSocket authentication. Treat the dashboard as a
# trusted local/operator UI; do not publish this value in a public build.
NEXT_PUBLIC_LIFTGUARD_API_KEY=
