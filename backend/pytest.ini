[pytest]
# Only the dedicated test suite: app/hardware/laser_test.py matches pytest's
# *_test.py collection pattern and imports pyserial, which the light
# requirements-dev.txt install deliberately omits.
testpaths = tests
